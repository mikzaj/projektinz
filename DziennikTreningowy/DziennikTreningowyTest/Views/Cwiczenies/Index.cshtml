@model IEnumerable<DziennikTreningowy.Models.Cwiczenie>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IloscPowtorzen)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NazwaCwiczenia)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IloscPowtorzen)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NazwaCwiczenia)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CwiczenieID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CwiczenieID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CwiczenieID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
